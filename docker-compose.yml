services:
  altplayer:
    build: .
    image: altplayer:dev
    container_name: altplayer
    ports:
      - "127.0.0.1:3000:3000"
    volumes:
      - ./data:/data
    env_file:
      - .env
    restart: unless-stopped
